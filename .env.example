# Cubit Backend Environment Variables

# Port for the API server
# Railway sets this automatically - don't override in Railway
PORT=8080

# CORS Configuration
# For development: use "*" to allow all origins
# For production: set to your frontend URL(s), comma-separated
# Example: https://your-frontend.railway.app,https://www.yourdomain.com
CORS_ORIGINS=*
