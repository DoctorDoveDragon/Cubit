# ==============================================================================
# Cubit Environment Variables Configuration
# ==============================================================================
# This file documents all environment variables used in the Cubit project.
# Copy this file to .env.local and customize values for your environment.

# ==============================================================================
# BACKEND CONFIGURATION (Python/FastAPI)
# ==============================================================================

# Port for the API server
# Railway sets this automatically - don't override in Railway
# Default: 8080
PORT=8080

# CORS Configuration
# Controls which origins can make API requests to the backend
# For development: use "*" to allow all origins
# For production: set to your frontend URL(s), comma-separated
# Examples:
#   - Development: *
#   - Production: https://your-frontend.railway.app
#   - Multiple: https://app.example.com,https://www.example.com
CORS_ORIGINS=*

# ==============================================================================
# FRONTEND CONFIGURATION (Next.js)
# ==============================================================================

# Backend API URL
# CRITICAL: This must be set for the frontend to connect to the backend
# IMPORTANT: Variables starting with NEXT_PUBLIC_ are embedded at build time
#            You must rebuild/redeploy after changing these values
# 
# For local development: http://localhost:8080
# For production: Your deployed backend URL (e.g., https://api.example.com)
# For Railway: https://your-backend-service.railway.app
NEXT_PUBLIC_API_URL=http://localhost:8080

# Node Environment
# Controls Next.js behavior and optimizations
# Values: development | production | test
# Default: development
NODE_ENV=development

# ==============================================================================
# DOCKER CONFIGURATION
# ==============================================================================

# Backend URL for Docker internal networking
# Used when frontend and backend communicate within Docker network
# Default: http://localhost:8080
BACKEND_URL=http://localhost:8080

# Puppeteer Configuration
# Skip Chromium download during npm install (reduces install time)
# Set to 'true' if you don't need Puppeteer's browser automation
# Default: true
PUPPETEER_SKIP_DOWNLOAD=true

# ==============================================================================
# OPTIONAL INTEGRATIONS
# ==============================================================================

# OpenAI API Key (if using OpenAI features)
# Get your key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-...

# Analytics (if implemented)
# NEXT_PUBLIC_ANALYTICS_ID=...

# ==============================================================================
# DEVELOPMENT TOOLS
# ==============================================================================

# Enable bundle analyzer for Next.js
# Set to 'true' to analyze bundle size during build
# Default: false
ANALYZE=false

# Debug logging
# Set to 'true' to enable verbose logging
# Default: false
DEBUG=false

# ==============================================================================
# PLATFORM-SPECIFIC VARIABLES
# ==============================================================================

# Railway
# These are typically set automatically by Railway
# PORT - Automatically set by Railway
# RAILWAY_ENVIRONMENT - Automatically set (e.g., 'production', 'staging')
# RAILWAY_GIT_COMMIT_SHA - Automatically set

# Vercel
# VERCEL_URL - Automatically set by Vercel
# VERCEL_ENV - Automatically set (e.g., 'production', 'preview', 'development')

# ==============================================================================
# NOTES
# ==============================================================================
# 
# 1. Local Development Setup:
#    - Copy this file to .env.local: cp .env.example .env.local
#    - For frontend: Create frontend/.env.local with NEXT_PUBLIC_API_URL
#    - Edit values as needed for your local setup
#
# 2. Production Deployment:
#    - Set environment variables in your deployment platform dashboard
#    - Never commit .env.local or files with secrets to version control
#    - Remember to redeploy after changing NEXT_PUBLIC_* variables
#
# 3. Docker Usage:
#    - Docker Compose will read .env.local if it exists
#    - You can also pass variables via docker-compose.yml
#    - For docker-compose, create .env in root directory
#
# 4. Security:
#    - Never commit API keys, tokens, or secrets to Git
#    - Use different values for development, staging, and production
#    - Rotate secrets regularly
#    - Use platform secret management when available
#
# ==============================================================================

