// This file exists as a placeholder to prevent Next.js from searching for
// or compiling config files that don't exist in the root directory.
// 
// The actual Next.js configuration is located in the frontend directory
// at frontend/next.config.ts
//
// This helps avoid potential errors when:
// - Build systems might look for config files in the wrong location
// - Someone accidentally runs Next.js commands from the root directory
// - Stale compiled configs could reference missing dependencies

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Empty configuration - frontend has the actual config
};

module.exports = nextConfig;
